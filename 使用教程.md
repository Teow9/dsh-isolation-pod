# 隔离舱 使用教程

面向第一次上手的人，从「装好了，然后呢」一路走到能熟练使用。

**读完你会做到**：配置沙箱 → 跑第一个隔离任务 → 看懂执行过程与思考过程 → 就同一会话继续追问 →
把结论送回主会话 → 把文件导出到工作区 → 清理，并且知道出问题该看哪里。

> 安装与原理见 [`README.md`](README.md)；功能需求见 [`需求说明.md`](需求说明.md)。
> 本教程里的界面文案、按钮名称、提示语都取自实际代码。

---

## 0. 开始前的检查清单

| 项 | 要求 |
|---|---|
| dsh | 已安装并能打开 Web 界面 |
| patch 行 | 已指向本仓库 `lib\index.js`（见 README「安装」） |
| 系统 | Windows（当前实现调用 PowerShell） |
| 沙箱目录 | 想好一个**工作区之外**的目录，例如 `E:\pod-work` |

打开 dsh 后，侧边栏「新会话」下方应出现一行：**盾牌图标 + 「隔离舱」**。
侧边栏收成窄栏时只剩图标，展开即可看到文字。

---

## 1. 认识界面

点「隔离舱」那一行，中栏切成隔离舱面板。顶部是四个页签：

| 页签 | 用途 |
|---|---|
| **任务** | 任务列表；点「查看」进入某个任务的详情（对话视图） |
| **新建** | 填任务描述，启动一个新隔离任务 |
| **配置** | 沙箱根目录、工具白名单、超时、并发等 |
| **导出记录** | 历次导出明细，可撤销复制模式的导出 |

顶部右侧显示 `主会话 session-xxxxxxxx…`，表示面板认出你当前所在的主会话；
若显示红色 `未定位主会话`，说明当前没有会话上下文，此时无法启动任务。

面板顶部可能出现四种状态条，含义各不相同：

| 状态条 | 含义 |
|---|---|
| `尚未配置沙箱根目录。隔离舱会拒绝执行任何任务（这是设计行为）。请到「配置」中手动指定。` | 还没配置，去「配置」页 |
| `沙箱根目录当前不存在：<路径>（启动任务时会创建）` | 正常提示，启动任务会自动创建 |
| `注意：沙箱根目录位于当前会话工作区之内，隔离任务生成的文件会出现在你看到的工作区里。` | **只有**当沙箱根目录落在**你正在看的这个会话**的工作区内才出现，提醒你会"看见"隔离产物 |
| `沙箱根目录：<路径>` | 一切正常 |

---

## 2. 第一步：配置沙箱根目录

第一次使用**必须**先做这件事——未配置时隔离舱会直接拒绝执行任务，
而且**不会**退而使用工作区或项目目录（这是刻意的设计，不是 bug）。

1. 切到 **配置** 页签；
2. 在「沙箱根目录」里填一个**绝对路径**，例如 `E:\pod-work`；
3. 确认下面几个勾选（默认值通常就够了）：
   - ☑ **允许文件写入（始终限制在沙箱内）** —— 关掉则隔离环境以只读模式运行；
   - ☑ **读取主会话上下文（只读，默认开启）** —— 允许它读主会话最近若干条消息来理解你的意图；
   - **超时（秒）** 默认 600，**并发上限** 默认 3；
   - ☑ 启用「导出到工作区」；
   - ☐ 允许「记住本次选择」——默认关闭，见 [§7](#7-把文件导出到工作区)；
   - **Agent preset** 默认 `standard`：隔离 Agent 的工具来自这个 preset，
     **留空则它一个工具都没有**。
4. 点 **保存配置**，看到「配置已保存」提示即成功。

下面的「允许隔离环境使用的工具」是一张复选框表：**没勾的工具会被硬性拒绝**
（隔离环境调用时直接报错，不是"劝它别用"）。

> 建议：沙箱目录**不要**放在工作区里面。否则面板会（正确地）警告你，
> 隔离产物会出现在你的工作区里，失去"眼不见为净"的意义。

---

## 3. 跑第一个任务

切到 **新建** 页签：

1. **任务描述**（必填）：写清目标与验收标准。它同时也是任务在列表里的标题（取前 40 字）。
2. **沙箱子目录**（可选）：填了就复用 `<沙箱根>\<子目录>`，不填则每个任务一个独立目录
   `<沙箱根>\<任务ID>`。子目录名不能包含 `\ / : * ? " < > |`。
3. 点 **启动隔离任务**。

### 任务描述怎么写

| | 例子 |
|---|---|
| ✅ 好 | `用 Python 生成一个 CSV 文件 report.csv，写入 3 行示例数据（表头 name,score），完成后汇报文件大小并回读校验内容` |
| ✅ 好 | `在沙箱里把 orders.csv 按城市分组统计金额小计，另存为 summary.csv，并告诉我哪个城市最高` |
| ❌ 差 | `处理一下数据` —— 没有产物、没有验收标准 |
| ❌ 差 | `把 E:\ 下的文件整理一下` —— 沙箱外不可写，必然失败 |

规矩很简单：**要写文件就写在沙箱里，要读什么都可以读**。

### 点下按钮后的几秒内发生了什么

1. 创建沙箱目录（不存在则自动建立）；
2. 开一个**独立子会话**（cwd = 沙箱根目录），写入沙箱模式与审批策略、挂上工具白名单与系统提示词；
3. 跑一次**越界写自检**：故意尝试往沙箱外写一个探针文件，**必须被系统拒绝**才继续；否则任务直接失败；
4. 把任务描述（附带沙箱规则、白名单、以及只读的主会话上下文片段）作为一条消息交给它；
5. 面板自动跳到该任务的详情页，开始增量显示执行过程。

任务列表中每一行显示：状态徽标、标题、创建时间、耗时、`查看` / `取消`（运行中才有）/ `清理`，
失败时还会直接显示错误文本。

---

## 4. 看懂任务详情页

点任务行里的 **查看**（或在详情页点 **返回列表** 回到列表）。从上到下：

### 4.1 头部

- 状态徽标：`成功` / `失败` / `超时` / `已取消` / `已中断` / `运行中` / `排队中`；
- `mode=workspace-write`：该任务实际生效的沙箱模式；
- `越界写已被拒绝`（绿色）：第 3 步的越界写自检通过，**这条标记是沙箱真的生效的证据**；
- `沙箱：<任务目录>`、`主会话上下文：N 条上下文`、`可用工具：...`；
- 失败时这里会显示红色错误文本。

> 关于 `主会话上下文：N 条上下文`：开关打开时，隔离环境会读到主会话最近若干条消息，
> 仅用于理解你的意图。它**只读**，不会修改主会话。

### 4.2 对话卡（核心）

标题行：`共 N 条 · 第 K 轮`，运行时显示 `运行中…`，右侧标出是否可继续对话：

| 右侧文字 | 含义 |
|---|---|
| `可继续对话` | 该会话的 Agent 还在本进程内，可以继续追问 |
| `进程重启后仅可查看` | dsh 重启过，历史仍可看、可导出，但**不能**继续对话 |

正文按条目类型渲染：

| 样子 | 是什么 |
|---|---|
| `第 N 轮开始 / 第 N 轮结束：completed` 分隔线 | 一轮对话的边界 |
| 右对齐 `我` 气泡 | 你发出的消息 |
| 左对齐 `隔离舱` 气泡 | 它的回答 |
| `▶ pwsh …` / `◀ …` | 工具调用与返回结果（截断显示） |
| `思考过程（N 字）` 可折叠块 | 模型的推理内容（reasoning），展开即可看它怎么想的 |
| `本轮发给隔离舱的指令（…）` 可折叠块 | 首轮那条机器指令原文，含沙箱规则与只读上下文；默认折叠以免刷屏 |

新条目会**自动滚到底部**；列表每 1.5 秒增量拉取一次，所以运行中也能看着它一步步干活。

### 4.3 输入框（继续对话）

见 [§5](#5-多轮追问实战)。

### 4.4 生成文件

点 **刷新** 拉取该任务沙箱目录的清单，每行是 `相对路径 + 类型 + 字节数`，带复选框（导出时用）。

> 当前面板**只列清单、不预览文件内容**。要看内容：直接在系统里打开那个路径（沙箱就是普通目录），
> 或用附录 B 的 `readSandboxFile` 接口。

### 4.5 返回主会话 / 导出到工作区 / 清理

分别在 §6、§7、§8 说明。

---

## 5. 多轮追问实战

详情页底部的输入框：**回车发送，Shift+回车换行**；运行中可点 **停止**。

### 一个完整的例子

| 轮次 | 你发的 | 它做的 |
|---|---|---|
| 第 1 轮 | `用 Python 生成 report.csv，3 行示例，表头 name,score` | 写脚本 → 运行 → 回读校验 → 汇报"40 字节，内容如下…" |
| 第 2 轮 | `再加 2 行数据，并按 score 降序排列` | **在同一个会话里**改脚本、重跑、重新校验 |
| 第 3 轮 | `把最终内容贴给我，并说明你怎么排序的` | 它记得前两轮的文件与决策，直接回答 |

关键点：追问走的是**同一个子会话**（同一个沙箱、同一套工具白名单、同一段系统提示词），
所以它记得之前做过什么，**也不存在"续聊时沙箱失效"的路径**——隔离规则挂在那个常驻 Agent 上原样复用。

### 什么时候不能续

dsh 一重启，隔离 Agent 就随进程消失了。此时：

- 详情页对话卡标题行右侧显示 `进程重启后仅可查看`，输入框变灰并提示
  `该会话已随进程结束，无法继续对话（历史仍可查看）`；
- 历史对话、思考过程、文件清单、导出**都还能用**；
- 想继续那个话题？到「新建」开一个新任务，把需求重新描述一遍即可
  （旧的沙箱文件还在，可以让新任务去读它们）。

---

## 6. 把结果送回主会话

在详情页的 **返回主会话** 区块：

1. **附带文件**（可选）：填生成文件清单里的一个相对路径；
2. 点 **生成预览**：得到一段可编辑文本，形如

   ```
   【隔离舱返回】用 Python 生成一个 CSV 文件 report.csv，写入 3 行示例
   状态：成功
   结论：已完成：在 E:\pod-work\pod-xxxx 下生成 report.csv（40 字节，UTF-8、LF）…
   ```

3. **自由编辑**这段文本（删掉不想要的、补上你的问题都行）；
4. 点 **确认返回主会话**，看到「已返回主会话」。

注入到主会话的是**一条用户消息**（来源标记为插件 `isolation-pod`，形式上是一次转交），
它会触发主会话的一轮响应——也就是说，你会看到它像收到你的提问一样开始处理。

> 默认不返回任何内容。**只有你点确认才会发生**，这是主会话"零痕迹"的兑现方式。
> 返回消息里也可以带上你对该结果的疑问，让主会话接着分析。

---

## 7. 把文件导出到工作区

导出与返回是**两个互不触发**的操作，可以只导出不返回，也可以只返回不导出。

1. 在 **生成文件** 区块勾选要导出的条目（可多选，文件夹也可以）；
2. 在 **导出到工作区** 区块填 **目标目录**：必须是**沙箱之外**的绝对路径
   （可以点 `选择` 用目录选择器）；
3. 选 **方式**：`复制`（保留沙箱原件，可撤销）或 `移动`（沙箱内删除，不可撤销）；
4. 选 **冲突**：`覆盖` / `跳过` / `重命名`；
5. 点 **导出到工作区** → 出现确认块 `确认导出 N 项到：<目录> ？` → 点 **确认**。

要点：

- **每次导出都要重新确认**，不存在自动延续的授权；
- 勾选「记住本次选择」后也**只是预填路径**，仍然每次确认（该项默认可在配置里关闭）；
- 复制模式可在 **导出记录** 页签点 `撤销此次导出` 删除已落盘的副本；
  移动模式显示 `移动模式不支持撤销`；
- 目标目录**不能**位于沙箱内（会被拒绝），这是为了避免"左右手互倒"造成混乱。

---

## 8. 清理

| 按钮 | 作用 | 影响范围 |
|---|---|---|
| `删除任务文件` | 删除该任务的沙箱目录 | 只删沙箱内的该任务目录；**拒绝**删除沙箱根目录本身 |
| `清理任务记录与文件` | 上面这条 + 删除记录 + 释放常驻 Agent | 面板里该任务消失 |
| 列表行上的 `清理` | 同上（`scope: all`） | 同上 |

清理**不会**影响主会话，也**不会**触碰沙箱之外的任何文件。
若只是想结束某轮正在跑的对话，用 **停止** 或列表行的 `取消`，不要用清理。

---

## 9. 五个实用配方

**① 只读分析**（只要结论，不要产物）
配置里取消 ☑「允许文件写入」→ 隔离环境以 `read-only` 运行，适合"帮我分析一下这个日志/表格"。

**② 收紧到只读工具**
在工具表里只勾 `read`、`glob`、`grep`（外加 `pwsh` 视需要）→ 未勾的调用直接报错，
比"劝它别写"可靠得多。

**③ 一个沙箱放多组产物**
用「沙箱子目录」区分，例如 `task-a`、`task-b`，产物落在 `<沙箱根>\task-a\` 等，互不覆盖。

**④ 长任务**
「超时（秒）」是**每轮**的超时，不是整段对话的总时长。跑大数据集时把它改大（例如 1800）。

**⑤ 换沙箱根目录**
随时可在配置里改成新目录：**新任务**用新目录，**旧任务的沙箱与记录完全不受影响**
（旧任务仍指向它创建时的目录）。改完记得回面板看一眼顶部状态条是否变成 `沙箱根目录：<新路径>`。

---

## 10. 出问题怎么查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 「启动隔离任务」是灰的 | 没配沙箱根目录 | 去「配置」填绝对路径并保存 |
| 顶部黄条说沙箱在工作区内 | 沙箱根目录确实落在当前会话工作区里 | 换到工作区外；若你**就是**想这样，忽略即可 |
| 点启动没反应、或立刻失败 | 沙箱根目录不是绝对路径 / 子目录名含非法字符 / 并发已达上限 | 看返回的提示文本，逐个修正 |
| 一直 `运行中…` 不动 | 模型在长时间思考，或工具在跑长命令 | 等；必要时点 **停止**，或用「取消」 |
| 状态 `超时` | 单轮超过「超时（秒）」 | 调大超时后重发该轮追问 |
| 状态 `失败` 且写着 `沙箱自检未通过` | 越界写没有被系统拒绝 | **不要绕过**：说明沙箱没生效，请检查 dsh 的文件沙箱配置 |
| 工具调用报"未被授权" | 该工具不在白名单里 | 到「配置」勾上它（或改用白名单内的工具） |
| 导出失败 | 目标目录为空 / 不是绝对路径 / 在沙箱内 / 没勾选条目 | 按提示修正；目标必须是沙箱之外的绝对路径 |
| 面板显示 `配置加载中…` 或红字横幅 | 接口不可用（令牌或连接问题） | 先刷新一次页面；仍不行则重启 dsh，并用附录 B 的 `/status` 确认 `ok: true` |
| 完全看不到「隔离舱」入口 | patch 行没生效，或加载失败 | 用附录 B 的 `/status` 判断：有响应=已挂载；无响应=检查 patch 行与 dsh 启动日志 |
| **dsh 起不来** | patch 行解析失败或永不激活（例如写进了非 Web profile） | 把 `cordis.patch.yml` 还原成 `[]` 或删除它，重启后再排查 |

---

## 11. 边界速查

**主会话绝对不会发生的事**

- 不会收到隔离任务的任何自动消息（只有你点「确认返回主会话」才会有那一条）；
- 不会因为隔离任务而在工作区里多出文件（除了你手动导出的那些）；
- 不会出现"隔离任务正在运行"之类的界面痕迹。

**隔离环境做不到的事**

- 不能写沙箱根目录之外的任何路径（系统级硬拒绝，试了会报错）；
- 不能调用白名单之外的工具；
- 不能自己把结果送回主会话、也不能自己导出文件——这两件事只有你能触发。

**它做得到的事**

- 读沙箱之外的任意路径（工作区、其它盘符）来理解上下文，但**只读**，不改源文件。

---

## 附录 A：任务状态含义

| 徽标 | 内部值 | 含义 |
|---|---|---|
| 成功 | `completed` | 这一轮正常结束并产出了结论 |
| 失败 | `failed` | 出错（含沙箱自检未通过、模型报错） |
| 超时 | `timeout` | 单轮超过配置的超时时间 |
| 已取消 | `cancelled` | 你点了停止/取消 |
| 已中断 | `interrupted` | dsh 重启时该任务还在运行，Agent 已消失 |
| 运行中 | `running` | 正在执行当前轮 |
| 排队中 | `queued` | 已创建、等待开跑 |

## 附录 B：命令行自检

**1) 看插件是否挂载、当前版本、沙箱与在线会话**

```powershell
curl.exe -s http://127.0.0.1:3080/isolation-pod/status
```

关注 `"ok": true`、`"build"`（每次 Host 半改动的递增标记）、`"sandbox": { "root": …, "liveSessions": […] }`。

**2) 直接调接口**（调试用；令牌在浏览器里取）

在 dsh 页面按 F12，控制台执行 `copy(window.__DSH_IPP__.token)` 取得令牌，然后：

```powershell
$t = '<粘贴令牌>'
curl.exe -s -X POST http://127.0.0.1:3080/isolation-pod/api `
  -H "content-type: application/json" -H "x-ipp-token: $t" `
  --data-binary '{"method":"getState","args":{}}'
```

常用：`listFiles`（`{"taskId":"pod-…"}`）、`readSandboxFile`（`{"taskId":"pod-…","relPath":"report.csv"}`，≤300 KB）、
`listExports`。方法全表见 README 的「HTTP 接口」。

**3) 沙箱目录直接看**

```powershell
Get-ChildItem 'E:\pod-work' -Recurse | Select-Object FullName,Length
```

## 附录 C：对照需求规格验收

想逐条验证 `需求说明.md` §6，按这个顺序走一遍最省事：

1. 清空沙箱配置 → 「启动隔离任务」按钮变灰，旁边提示「需要先配置沙箱根目录。」；
   若绕过界面直接调接口，Host 也会以 `NO_SANDBOX_ROOT` 拒绝（**不会**自动改用工作区或项目目录）；
2. 配好沙箱 → 跑「用 Python 生成一个 CSV」→ 检查主会话工作区**无新增文件**，沙箱内**有**该文件；
3. 全程留意主会话：不应出现任何自动消息与界面痕迹；
4. 面板内能查看完整执行记录（含思考过程）与生成文件清单；
5. 手动「返回主会话」→ 主会话才出现一条精简结果；
6. 手动「导出到工作区」→ 文件才出现在你指定的路径；导出与返回互不触发；
7. 连续导出两次：每次都要求重新确认；
8. 勾选一次「记住本次选择」（需先在配置里打开），确认它仍需你每次点确认；
9. 让隔离任务尝试写沙箱外的路径 → 应被拒绝，且任务记录里 `越界写已被拒绝` 为真；
10. 让隔离任务**读**沙箱外的文件（如工作区里的某个 md）→ 读得到，且源文件修改时间不变；
11. 改沙箱根目录 → 新任务用新目录，旧沙箱与旧任务不受影响；
12. 不返回、不导出地跑一个任务 → 主会话全程无感。
