# 隔离舱使用教程

> 从零上手的分步走查：界面导览 → 第一个任务 → 多轮追问 → 返回与导出 → 排障 → 逐条验收。
> 安装与原理见 [`README.md`](README.md)；功能需求见 [`需求说明.md`](需求说明.md)。
> 本教程里的界面文案、按钮名称、提示语都取自实际代码。

**对应版本**：插件 0.1.2 · DSH 0.1.7（桌面版 Desktop 0.1.7-rc.2 与 `dsh web` 都适用）。

---

## 0. 开始前的检查清单

| 项 | 要求 |
|---|---|
| DSH | **0.1.7**，已安装并能打开 Web 界面（桌面版或 `dsh web` 均可） |
| patch 行 | 已指向本仓库 `lib\index.js`（见 README「安装」；桌面版在 `profiles\desktop\cordis.patch.yml`） |
| 系统 | Windows（当前实现调用 PowerShell；没有 `bash` 工具，白名单以 `pwsh` 为准） |
| 沙箱目录 | 想好一个**工作区之外**的目录，例如 `E:\pod-work`（不要用盘符根，见 [§10](#10-出问题怎么查)） |

打开 DSH 后，侧边栏「新会话」下方应出现一行：**盾牌图标 + 「隔离舱」**。
侧边栏收成窄栏时只剩图标，展开即可看到文字。

> **桌面版**：保存 patch 行即热激活 Host 半，客户端入口也会经模块图推送**自动挂载**，都不需要重启或刷新。
> 若面板一打开就提示令牌不可用，让它自己重试即可（它会先取 `/isolation-pod/bootstrap`，再自动重载一次）；
> 仍失败就退出并重新打开应用——release 版桌面**没有**「刷新页面」菜单。

---

## 1. 认识界面

点「隔离舱」那一行，中栏切成隔离舱面板。顶部是四个页签：

| 页签 | 用途 |
|---|---|
| **任务** | 任务列表；点「查看」进入某个任务的详情（对话视图） |
| **新建** | 填任务描述，启动一个新隔离任务 |
| **配置** | 沙箱根目录、工具白名单、超时、并发、Agent preset 等 |
| **导出记录** | 历次导出明细，可撤销复制模式的导出 |

顶部右侧显示 `主会话 session-xxxxxxxx…`，表示面板认出你当前所在的主会话（它按"主视图正在看哪个会话"
解析，所以打开本面板也不会丢失）。若显示 `未定位主会话`，说明当前没有活跃会话——
此时点「启动隔离任务」会被拒绝并提示 `未定位到当前主会话，无法启动`。

面板顶部可能出现四种状态条，含义各不相同：

| 状态条 | 含义 |
|---|---|
| `尚未配置沙箱根目录。隔离舱会拒绝执行任何任务（这是设计行为）。请到「配置」中手动指定。` | 还没配置，去「配置」页 |
| `沙箱根目录当前不存在：<路径>（启动任务时会创建）` | 正常提示，启动任务会自动创建 |
| `注意：沙箱根目录位于当前会话工作区之内，隔离任务生成的文件会出现在你看到的工作区里。` | **只有**当沙箱根目录落在**你正在看的这个会话**的工作区内才出现 |
| `沙箱根目录：<路径>` | 一切正常 |

若接口暂时不可用（令牌或连接问题），面板顶部会出现警示色横幅 `隔离舱接口暂时不可用：<原因>`，
并带一个 **重新载入页面** 按钮——此时下方内容停在加载态，不会静默失败。

---

## 2. 第一步：配置沙箱根目录

第一次使用**必须**先做这件事——未配置时隔离舱会直接拒绝执行任务，
而且**不会**退而使用工作区或项目目录（这是刻意的设计，不是 bug）。

1. 切到 **配置** 页签；
2. 在「沙箱根目录」里填一个**绝对路径**，例如 `E:\pod-work`
   （输入框提示语就是 `必须手动指定绝对路径，例如 E:\pod-work`）；
3. 确认下面几项（默认值通常就够了）：
   - ☑ **允许文件写入（始终限制在沙箱内）** —— 关掉则隔离环境以 `read-only` 模式运行；
   - ☑ **读取主会话上下文（只读，默认开启）** —— 允许它读主会话最近若干条消息来理解你的意图；
   - **超时（秒）** 默认 600（这是**每轮**的超时，可设 30–7200），**并发上限** 默认 3（可设 1–8）；
   - ☑ 启用「导出到工作区」；
   - ☐ 允许「记住本次选择」——默认关闭，见 [§7](#7-把文件导出到工作区)；
   - **Agent preset** 默认 `standard`：隔离 Agent 的工具来自这个 preset，**留空则它一个工具都没有**。
     可用时该字段是下拉框（列出注册表里的 preset，不可用的会标「不可用」）。
4. 点 **保存配置**，看到「配置已保存」提示即成功。

下面的「允许隔离环境使用的工具」是一张复选框表：**未勾选的工具会被硬性拒绝**
（隔离环境调用时直接报错，不是"劝它别用"）。注意它只是**上限**——真正能用的还要看 preset 提供了什么，
两者取交集的结果会显示在任务详情的「可用工具」与「隔离环境实有」两行里。

> 建议：沙箱目录**不要**放在工作区里面。否则面板会（正确地）警告你，
> 隔离产物会出现在你的工作区里，失去"眼不见为净"的意义。

---

## 3. 跑第一个任务

切到 **新建** 页签：

1. **任务描述**（必填）：写清目标与验收标准。它同时也是任务在列表里的标题（取前 40 字）。
2. **沙箱子目录**（可选，占位符 `可选，例如 task-a`）：填了就复用 `<沙箱根>\<子目录>`，
   不填则每个任务一个独立目录 `<沙箱根>\<任务ID>`。子目录名不能包含 `\ / : * ? " < > |`。
3. 点 **启动隔离任务**。

按钮下方会说明：`任务在主会话之外运行；主会话不会收到任何自动消息。`
（沙箱根目录未配置时该按钮是灰的，旁边提示 `需要先配置沙箱根目录。`）

### 任务描述怎么写

| | 例子 |
|---|---|
| ✅ 好 | `用 Python 生成一个 CSV 文件 report.csv，写入 3 行示例数据（表头 name,score），完成后汇报文件大小并回读校验内容` |
| ✅ 好 | `在沙箱里把 orders.csv 按城市分组统计金额小计，另存为 summary.csv，并告诉我哪个城市最高` |
| ❌ 差 | `处理一下数据` —— 没有产物、没有验收标准 |
| ❌ 差 | `把 E:\ 下的文件整理一下` —— 沙箱外不可写，必然失败 |

规矩很简单：**要写文件就写在沙箱里，要读什么都可以读**。

### 点下按钮后的几秒内发生了什么

1. 创建沙箱目录（不存在则自动建立）；
2. 开一个**独立子会话**（cwd = 沙箱根目录），写入沙箱模式与审批策略、挂载 preset、
   按 pod 的真实工具面收敛白名单，并装上工具 guard 与系统提示词；
3. 跑一次**越界写自检**：往沙箱根目录的**上一级**写一个探针文件，**必须被系统拒绝**才继续；
   没被拒绝（或结果不明确、模式不符）任务就直接失败，并把原因写进错误文本；
4. 把任务描述（附带沙箱规则、白名单、以及只读的主会话上下文片段）作为一条消息交给它；
5. 面板自动跳到该任务的详情页，开始增量显示执行过程。

任务列表中每一行显示：状态徽标、标题、创建时间、耗时、`查看` / `取消`（运行中才有）/ `清理`，
失败时还会直接显示错误文本。

---

## 4. 看懂任务详情页

点任务行里的 **查看**（或在详情页点 **返回列表** 回到列表）。从上到下：

### 4.1 头部

- 状态徽标：`成功` / `失败` / `超时` / `已取消` / `已中断` / `运行中` / `排队中`；
- `mode=workspace-write`：该任务实际生效的沙箱模式；
- `越界写已被拒绝`（绿色）：第 3 步的越界写自检通过，**这条标记是沙箱真的生效的证据**；
- `沙箱：<任务目录>`、`主会话上下文：N 条上下文`；
- **可用工具**：白名单与 preset 工具面收敛后的结果；
  为空时显示 `（无：当前 preset 未提供白名单里的任何工具）`；
- **隔离环境实有**：该 pod 实际能看到的工具面（由 preset 决定），用来解释"勾了却用不了"；
- preset 挂载失败等 setup 问题会以警示色 chip 显示；任务失败时是红色错误文本。

> 关于 `主会话上下文：N 条上下文`：开关打开时，隔离环境会读到主会话最近若干条消息（只读），
> 仅用于理解你的意图，不会修改主会话。

### 4.2 对话卡（核心）

标题行：`共 N 条 · 第 K 轮`，运行时显示 `运行中…`，右侧标出是否可继续对话：

| 右侧文字 | 含义 |
|---|---|
| `可继续对话` | 该会话的 Agent 还在本进程内，可以继续追问 |
| `进程重启后仅可查看` | 宿主重启过，历史仍可看、可导出，但**不能**继续对话 |

正文按条目类型渲染：

| 样子 | 是什么 |
|---|---|
| `第 N 轮开始 / 第 N 轮结束：completed` 分隔线 | 一轮对话的边界 |
| 右对齐 `我` 气泡 | 你发出的消息 |
| 左对齐 `隔离舱` 气泡 | 它的回答 |
| `▶ pwsh …` / `◀ …` | 工具调用与返回结果（截断显示） |
| `思考过程（N 字）` 可折叠块 | 模型的推理内容（reasoning），展开即可看它怎么想的 |
| `本轮发给隔离舱的指令（含沙箱规则与只读的主会话上下文）` 可折叠块 | 首轮那条机器指令原文，默认折叠以免刷屏 |

新条目会**自动滚到底部**；列表每 1.5 秒增量拉取一次，所以运行中也能看着它一步步干活。
还没开始时显示 `等待隔离舱开始…`，完全没有记录时显示 `（暂无记录）`。

### 4.3 输入框（继续对话）

见 [§5](#5-多轮追问实战)。

### 4.4 生成文件

点 **刷新** 拉取该任务沙箱目录的清单，每行是 `相对路径 + 类型 + 字节数`，带复选框（导出时用）。
尚未拉取时显示 `（暂无文件，或尚未刷新）`。

文件行右侧有 **预览** 按钮：点开就在下方就地显示该文件内容（等宽字体、可滚动），标题行标出字节数，
被截断时提示 `已截断显示`；再点一次 **收起** 或 **关闭** 即隐藏。

> 预览有硬上限：单文件 ≤**300 KB**，面板一次最多显示 **20000 字符**。超过 300 KB 会直接拒绝并说明原因；
> 更大的文件请在系统里打开——沙箱就是普通目录。

### 4.5 返回主会话 / 导出到工作区 / 清理

分别在 §6、§7、§8 说明。

---

## 5. 多轮追问实战

详情页底部的输入框：**回车发送，Shift+回车换行**；运行中可点 **停止**。
输入框占位符随状态变化：`继续对话 —— 回车发送，Shift+回车换行` /
`隔离舱正在运行，等它回应或点「停止」` / `该会话已随进程结束，无法继续对话（历史仍可查看）`。

### 一个完整的例子

| 轮次 | 你发的 | 它做的 |
|---|---|---|
| 第 1 轮 | `用 Python 生成 report.csv，3 行示例，表头 name,score` | 写脚本 → 运行 → 回读校验 → 汇报"40 字节，内容如下…" |
| 第 2 轮 | `再加 2 行数据，并按 score 降序排列` | **在同一个会话里**改脚本、重跑、重新校验 |
| 第 3 轮 | `把最终内容贴给我，并说明你怎么排序的` | 它记得前两轮的文件与决策，直接回答 |

关键点：追问走的是**同一个子会话**（同一个沙箱、同一套工具白名单、同一段系统提示词），
所以它记得之前做过什么，**也不存在"续聊时沙箱失效"的路径**——隔离规则挂在那个常驻 Agent 上原样复用。
输入框下方的提示即为此意：`沙箱与工具白名单在整段对话中保持不变；主会话不会自动收到任何内容。`

### 什么时候不能续

宿主一重启，隔离 Agent 就随进程消失了。此时：

- 详情页对话卡标题行右侧显示 `进程重启后仅可查看`，输入框变灰；
- 历史对话、思考过程、文件清单、导出**都还能用**；
- 想继续那个话题？到「新建」开一个新任务，把需求重新描述一遍即可
  （旧的沙箱文件还在，可以让新任务去读它们）。

---

## 6. 把结果送回主会话

在详情页的 **返回主会话** 区块（上方说明：`默认不返回任何内容。以下内容会以一条消息注入主会话，
注入前你可以自由编辑。导出文件与返回消息是两个互不触发的操作。`）：

1. **附带文件**（可选，占位符 `可选：清单中的相对路径`）：填生成文件清单里的一个相对路径；
2. 点 **生成预览**：得到一段可编辑文本，形如

   ```
   【隔离舱返回】用 Python 生成一个 CSV 文件 report.csv，写入 3 行示例
   状态：成功
   结论：已完成：在 E:\pod-work\pod-xxxx 下生成 report.csv（40 字节，UTF-8、LF）…
   ```

3. **自由编辑**这段文本（删掉不想要的、补上你的问题都行）；
4. 点 **确认返回主会话**，看到「已返回主会话」。

注入主会话的是**一条普通用户消息**，因此会触发主会话的一轮响应——你会看到它像收到你的提问一样开始处理。

> 默认不返回任何内容。**只有你点确认才会发生**，这是主会话"零痕迹"的兑现方式。
> 返回消息里也可以带上你对该结果的疑问，让主会话接着分析。

---

## 7. 把文件导出到工作区

导出与返回是**两个互不触发**的操作，可以只导出不返回，也可以只返回不导出。

1. 在 **生成文件** 区块勾选要导出的条目（可多选，文件夹也可以）；
2. 在 **导出到工作区** 区块填 **目标目录**：必须是**沙箱之外**的绝对路径
   （输入框占位符就是 `绝对路径`，可以点 `选择` 用目录选择器）；
3. 选 **方式**：`复制`（保留沙箱原件，可撤销）或 `移动`（沙箱内删除，不可撤销）；
4. 选 **冲突**：`覆盖` / `跳过` / `重命名`；
5. 点 **导出到工作区** → 出现确认块 `确认导出 N 项到：<目录> ？` → 点 **确认**
   （看到「导出完成：N 项」即成功）。

要点：

- **每次导出都要重新确认**，不存在自动延续的授权；区块内也写明
  `每次导出都需要手动确认，不存在自动延续的授权。`；
- 勾选「记住本次选择（仅预填路径，仍需每次确认）」后也**只是预填路径**，
  该项默认可在配置里关闭（☐ 允许「记住本次选择」）；
- 复制模式可在 **导出记录** 页签点 `撤销此次导出` 删除已落盘的副本，成功后标记 `已撤销`；
  移动模式显示 `移动模式不支持撤销`；
- 目标目录**不能**位于沙箱内（会被拒绝），这是为了避免"左右手互倒"造成混乱；
- 还没有任何导出记录时，该页签显示 `还没有导出记录。`。

---

## 8. 清理

| 按钮 | 作用 | 影响范围 |
|---|---|---|
| `删除任务文件` | 删除该任务的沙箱目录 | 只删沙箱内的该任务目录；**拒绝**删除沙箱根目录本身 |
| `清理任务记录与文件` | 上面这条 + 删除记录 + 释放常驻 Agent | 面板里该任务消失 |
| 列表行上的 `清理` | 同上（`scope: all`） | 同上 |

清理**不会**影响主会话，也**不会**触碰沙箱之外的任何文件。
若只是想结束某轮正在跑的对话，用 **停止** 或列表行的 `取消`，不要用清理。

---

## 9. 五个实用配方

**① 只读分析**（只要结论，不要产物）
配置里取消 ☑「允许文件写入」→ 隔离环境以 `read-only` 运行，适合"帮我分析一下这个日志/表格"。

**② 收紧到只读工具**
在工具表里只勾 `read`、`glob`、`grep`（外加 `pwsh` 视需要）→ 未勾的调用直接报错，
比"劝它别写"可靠得多。

**③ 一个沙箱放多组产物**
用「沙箱子目录」区分，例如 `task-a`、`task-b`，产物落在 `<沙箱根>\task-a\` 等，互不覆盖。

**④ 长任务**
「超时（秒）」是**每轮**的超时，不是整段对话的总时长。跑大数据集时把它改大（例如 1800）。

**⑤ 换沙箱根目录**
随时可在配置里改成新目录：**新任务**用新目录，**旧任务的沙箱与记录完全不受影响**
（旧任务仍指向它创建时的目录）。改完记得回面板看一眼顶部状态条是否变成 `沙箱根目录：<新路径>`。

---

## 10. 出问题怎么查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 「启动隔离任务」是灰的 | 没配沙箱根目录 | 去「配置」填绝对路径并保存 |
| 顶部提示 `未定位主会话` | 当前没有活跃会话 | 先在侧边栏打开/新建一个会话再回面板 |
| 顶部黄条说沙箱在工作区内 | 沙箱根目录确实落在当前会话工作区里 | 换到工作区外；若你**就是**想这样，忽略即可 |
| 点启动没反应、或立刻失败 | 沙箱根目录不是绝对路径 / 子目录名含非法字符 / 并发已达上限 | 看返回的提示文本，逐个修正 |
| 一直 `运行中…` 不动 | 模型在长时间思考，或工具在跑长命令 | 等；必要时点 **停止**，或用「取消」 |
| 状态 `超时` | 单轮超过「超时（秒）」 | 调大超时后重发该轮追问 |
| 状态 `失败` 且写着 `沙箱自检未通过` | 越界写没有被系统拒绝，或探测结果不明确 | **不要绕过**：说明沙箱没生效，请检查 DSH 的文件沙箱配置 |
| 头部没有 `越界写已被拒绝` 标记 | 沙箱根目录设成了盘符根（如 `E:\`），没有更上一级可探，自检被跳过 | 改成一个子目录（如 `E:\pod-work`）即可恢复自检 |
| 工具调用报"未被授权" | 该工具不在白名单里，**或所选 preset 不提供它** | 看任务详情的「隔离环境实有」：为空说明 preset 没有这类工具，换 preset 或改用白名单内的工具 |
| 导出失败 | 目标目录为空 / 不是绝对路径 / 在沙箱内 / 没勾选条目 | 按提示修正；目标必须是沙箱之外的绝对路径 |
| 面板显示 `配置加载中…` 或警示色横幅 | 接口不可用（令牌或连接问题） | 面板会自动取令牌并重试一次；仍不行点横幅里的「重新载入页面」，桌面版则退出并重新打开应用。用附录 B 的 `/status` 确认 `compat.bootstrap` 与 `ok: true` |
| 完全看不到「隔离舱」入口 | patch 行没生效，或加载失败 | 用附录 B 的 `/status` 判断：有响应=已挂载；无响应=检查 patch 行与宿主日志 |
| patch 行改了但行为没变 | 改的是 Host 半（`lib\index.js`），Node 按 URL 缓存 ES 模块 | 重启应用；只有客户端半（`lib\client.js`）与 patch 行本身是热生效的 |
| 未激活告警 | 该行在某个没有 `webServer` 的 profile 里 | 0.1.7 只告警、不锁死启动；把该行移出 home 级 patch / CLI profile 即可 |

---

## 11. 边界速查

**主会话绝对不会发生的事**

- 不会收到隔离任务的任何自动消息（只有你点「确认返回主会话」才会有那一条）；
- 不会因为隔离任务而在工作区里多出文件（除了你手动导出的那些）；
- 不会出现"隔离任务正在运行"之类的界面痕迹。

**隔离环境做不到的事**

- 不能写沙箱根目录之外的任何路径（系统级硬拒绝，试了会报错）；
- 不能调用白名单之外、或 preset 没有提供的工具；
- 不能自己把结果送回主会话、也不能自己导出文件——这两件事只有你能触发。

**它做得到的事**

- 读沙箱之外的任意路径（工作区、其它盘符）来理解上下文，但**只读**，不改源文件。

---

## 附录 A：任务状态含义

| 徽标 | 内部值 | 含义 |
|---|---|---|
| 成功 | `completed` | 这一轮正常结束并产出了结论 |
| 失败 | `failed` | 出错（含沙箱自检未通过、模型报错） |
| 超时 | `timeout` | 单轮超过配置的超时时间 |
| 已取消 | `cancelled` | 你点了停止/取消 |
| 已中断 | `interrupted` | 宿主在任务运行中重启，Agent 已消失 |
| 运行中 | `running` | 正在执行当前轮 |
| 排队中 | `queued` | 已创建、等待开跑 |

## 附录 B：命令行自检

**1) 看插件是否挂载、当前版本、沙箱与在线会话**

```powershell
curl.exe -s http://127.0.0.1:19387/isolation-pod/status   # 桌面版（固定 19387）
curl.exe -s http://127.0.0.1:3080/isolation-pod/status    # dsh web（以启动时打印的 URL 为准）
```

关注 `"ok": true`、`"build"` 与 `"pluginVersion"`（版本与改动标记）、
`"compat"`（`shellApi` 应为 `execute`；`bootstrap` 为 `true` 说明令牌通道可用）、
`"sandbox": { "root": …, "liveSessions": […] }`。

**2) 直接调接口**（调试用；令牌在浏览器里取）

在 DSH 页面按 F12，控制台执行 `copy(window.__DSH_IPP__.token)` 取得令牌，然后：

```powershell
$t = '<粘贴令牌>'
curl.exe -s -X POST http://127.0.0.1:19387/isolation-pod/api `
  -H "content-type: application/json" -H "x-ipp-token: $t" `
  --data-binary '{"method":"getState","args":{}}'
```

常用：`listFiles`（`{"taskId":"pod-…"}`）、`readSandboxFile`
（`{"taskId":"pod-…","relPath":"report.csv"}`，单文件 ≤300 KB）、`listExports`。
方法全表见 README 的「HTTP 接口」。

**3) 令牌通道是否关闭**（两条都应被拒绝）

```powershell
curl.exe -s -o NUL -w '%{http_code}\n' http://127.0.0.1:19387/isolation-pod/bootstrap     # 期望 401
curl.exe -s -o NUL -w '%{http_code}\n' -X POST http://127.0.0.1:19387/isolation-pod/api `
  -H 'content-type: application/json' --data-binary '{"method":"getState","args":{}}'      # 期望 403
```

**4) 沙箱目录直接看**

```powershell
Get-ChildItem 'E:\pod-work' -Recurse | Select-Object FullName,Length
```

## 附录 C：对照需求规格验收

想逐条验证 `需求说明.md` §6，按这个顺序走一遍最省事：

1. 清空沙箱配置 → 「启动隔离任务」按钮变灰，旁边提示「需要先配置沙箱根目录。」；
   若绕过界面直接调接口，Host 也会以 `NO_SANDBOX_ROOT` 拒绝（**不会**自动改用工作区或项目目录）；
2. 配好沙箱 → 跑「用 Python 生成一个 CSV」→ 检查主会话工作区**无新增文件**，沙箱内**有**该文件；
3. 全程留意主会话：不应出现任何自动消息与界面痕迹；
4. 面板内能查看完整执行记录（含思考过程）与生成文件清单；
5. 手动「返回主会话」→ 主会话才出现一条精简结果；
6. 手动「导出到工作区」→ 文件才出现在你指定的路径；导出与返回互不触发；
7. 连续导出两次：每次都要求重新确认；
8. 勾选一次「记住本次选择」（需先在配置里打开），确认它仍需你每次点确认；
9. 让隔离任务尝试写沙箱外的路径 → 应被拒绝，且任务记录里 `越界写已被拒绝` 为真；
10. 让隔离任务**读**沙箱外的文件（如工作区里的某个 md）→ 读得到，且源文件修改时间不变；
11. 改沙箱根目录 → 新任务用新目录，旧沙箱与旧任务不受影响；
12. 不返回、不导出地跑一个任务 → 主会话全程无感；
13. 任务详情里「可用工具」（白名单）与「隔离环境实有」（preset 工具面）应当一致地解释
    guard 的行为：勾了但 preset 没有的工具，调用会被打回。

> 与规格的差异（清理只按任务、撤销只支持复制模式等）逐条列在 README 的
> 「规格与本实现的差异」一节，验收时请以那里为准。
